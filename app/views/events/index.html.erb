<%= javascript_include_tag 'application' %>
<h1>予約／予約変更／キャンセル</h1>

<div class="col-sm-9 col-sm-offset-1">
<div class="container">
<%= link_to 'ご予約リクエスト', new_user_event_path(current_user), remote: true, class: "btn btn-md btn-primary" %>
<p></p>
<table class="table table-bordered table-condensed table-striped table-custom">
  <thead>
    <tr>   
      <th class="center">ご希望の時間</th>
      <th class="center">ご希望の内容</th>
      <th class="center">ご希望のスタイリスト</th>
      <th class="center">リクエスト状況</th>
      <th class="center">変更</th>
      <th class="center">削除</th>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td class="center"><%= l(event.started_at, format: :custom) %></td>
        <td class="center"><%= event.title %></td>
        <td class="center"><%= event.selector_stylist_request %></td>
        <td class="center" style="color: #F00; font-weight: 900;"><%= event.status_event_request %></td>
        <td class="center"><%= link_to '変更', edit_user_event_path(current_user, event), remote: true, class: "btn btn-xs btn-primary" %></td>
        <td class="center"><%= link_to '削除', user_event_path(current_user, event), method: :delete, data: { confirm: '削除してもよろしいでしょうか?' }, class: "btn btn-xs btn-danger" %></td>
      </tr>
      <td class="center">ご要望など</td>
      <td colspan="5"><%= event.note %></td>
    <% end %>
  </tbody>
</table>
</div>
<p class="center">☆ご予約リクエストが完了するまでに約1-2日お時間をいただいております☆</p>
<p class="center">☆ご予約のキャンセルは予約日の2日前までにお願いいたします☆</p>
<p class="center">☆定休日は毎週火曜日です☆</p>
<div class="container container-calendar">
<%= month_calendar events: @events do |date, events| %>
  <%= date.day %>
  <% events.each do |event| %>
    <%= event.started_at.hour %>:<%= event.started_at.min %>
    <%= event.title %>
  <% end %>
<% end %>
</div>
</div>

<div class="col-sm-9 col-sm-offset-1">
<div class="container">
<p class="center">☆その他のお問い合わせは<%= mail_to "admin@email.com", "こちら" %>までメールをお願いします☆</p>

<p class="center">　　　　　</p>
<p class="center">　　　　　</p>
<p class="center">　　　　　</p>
</div>
</div>

<!--モーダルウインドウ表示-->
<div id="event-new" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true"></div>
<div id="event-edit" class="modal fade" tabindex="-1" role="dialog" aria-hidden="true"></div>
